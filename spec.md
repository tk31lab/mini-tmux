# mini-tmux 仕様書

## 概要

ターミナルマルチプレクサ(tmux)のミニ版。まずは「1セッション・1ペインだが、
バックグラウンドで動き続け、いつでもデタッチ/再アタッチできる」というMVPを目指す。
将来的に複数ペイン/複数ウィンドウへ拡張することを見据えた設計にする。

## 目的・学習ゴール

- 疑似端末(pty)の生成と、master/slave間のデータ中継の仕組みを理解する
- termios制御によるraw modeの扱いを理解する
- デーモン化(バックグラウンドプロセス化)とUnixドメインソケットによる
  クライアント/サーバー通信を理解する
- ウィンドウサイズ変更(SIGWINCH)の伝搬など、端末まわりのシグナル処理を理解する

## 対象環境

- Unix系OS(Linux / macOS)を対象。pty関連APIはPOSIX準拠のものを優先して使う

## アーキテクチャ概要

tmuxと同様、クライアント/サーバーモデルを採用する。

- **サーバープロセス**: 実際のシェル(や任意のコマンド)をptyのslave側で起動し、
  ptyのmaster側を保持し続ける。セッションの状態を保持し、Unixドメインソケットで
  クライアントからの接続を待ち受ける。
- **クライアントプロセス**: ユーザーが直接操作するターミナル上で動く。
  自分の標準入出力をraw modeにし、Unixドメインソケット経由でサーバーに入力を送り、
  サーバーからの出力を受け取って自分の標準出力にそのまま流す。
- **セッション**: サーバーが管理する「1つのシェルとそのpty」の単位。
  名前で識別する(例: `mini-tmux new -s work`)。
- **アタッチ/デタッチ**: クライアントは特定のキー(例: `Ctrl-b d`)でサーバーとの
  接続を切る(デタッチ)。サーバー側のシェルはそのまま動き続ける。
  再度クライアントを起動して同じセッションに接続すると、続きから操作できる(アタッチ)。

## 機能要件(MVPスコープ)

- `mini-tmux new -s <name>`: 新しいセッションを作成する。サーバーが未起動なら起動し、
  サーバーはptyでシェル(`$SHELL` または `/bin/sh`)を起動して、クライアントとしてアタッチする
- `mini-tmux attach -s <name>`: 既存セッションにクライアントとしてアタッチする
- `mini-tmux ls`: 起動中のセッション一覧を表示する
- デタッチ: 特定のキーシーケンスでクライアントだけが切断され、サーバー側のシェルは
  実行され続ける
- クライアントの標準入力をそのままptyに転送し、ptyからの出力をそのまま
  クライアントの標準出力に転送する(この段階では複数ペインなし、素通し)
- クライアント側の端末サイズ変更(SIGWINCH)を検知し、サーバー経由でptyの
  ウィンドウサイズ(`TIOCSWINSZ`)に反映する
- サーバーが異常終了した場合、クライアントはエラーを表示して終了する

## 追加機能(拡張スコープ、優先度順)

1. 専用キーシーケンスによるデタッチ(`Ctrl-b d`)
   - 現状、デタッチする手段はクライアントプロセスを終了させること(ターミナルを
     閉じる、別プロセスからkillする)しかない。セッション内で完結する操作で
     デタッチできるようにする
   - tmuxと同じく「prefixキー(`Ctrl-b`)を押した次の1バイトをコマンドとして
     解釈する」方式にする。クライアントは中継ループ内で入力バイト列を見張り、
     prefix待ち状態かどうかを持つ必要がある
   - prefixに続くキーが`d`ならデタッチ、それ以外なら`Ctrl-b`ごとそのまま
     シェルに転送する(`Ctrl-b`自体をシェルに送りたい場合の逃げ道も要検討)
   - プロトコル側は `ClientMessage::Detach` を定義済みで、サーバーも受信時に
     クライアントを切ってシェルを生かしたまま次のattachを待つ実装になっている。
     残っているのはクライアント側のキー検出のみ
2. セッションの明示的なkill(`mini-tmux kill-session -s <name>`)
3. 複数ウィンドウ(1セッション内で複数のシェルを持ち、切り替えられる)
4. 複数ペイン(画面分割) — 各ペインの出力をANSIエスケープシーケンスごと解釈し、
   仮想スクリーンバッファとして保持する簡易ターミナルエミュレータの実装が必要になる
5. デタッチ中の出力をスクロールバックとして保持し、再アタッチ時に直近の画面を復元する
6. ステータスバー(セッション名、時刻などを画面下部に表示)

## 非スコープ(MVPではやらないこと)

- 複数ペイン・複数ウィンドウの同時表示(拡張スコープへ)
- tmux設定ファイル(`.tmux.conf`)相当の設定機構
- コピーモードなど、tmuxの高度な操作機能

## 既知の制限・セキュリティ上の注意(直さない前提で記録)

- **ソケットパスへのアクセス制御がない**: `session::socket_path(name)`は
  `std::env::temp_dir().join(format!("mini-tmux-{name}.sock"))`という、
  セッション名から機械的に決まるパスを返すだけで、mini-tmux自身は誰が
  接続してきたかを一切チェックしていない。
  - macOSでは`std::env::temp_dir()`がユーザーごとの`0700`ディレクトリを
    返すため、他ユーザーからのアクセスは偶然OS側で防がれている。
  - しかしLinuxでは`std::env::temp_dir()`は通常ただの`/tmp`
    (world-writable)を返す。この場合、同じマシンの**別ユーザーが
    セッション名を推測できれば、ソケットに直接繋いで任意の入力を
    注入したり出力を覗き見たりできてしまう**。
  - さらに、正規のクライアントがデタッチした直後に別プロセスが
    先んじて`connect`していた場合、次の`accept()`でその接続が
    先に処理される(セッションを横取りされる)レース条件も理論上ある。
  - 本家tmuxは`/tmp/tmux-<uid>/`のようにUID付き・`0700`のディレクトリを
    自前で作ることでこれを防いでいるが、mini-tmuxではスコープ外として
    意図的に対応していない。学習目的のミニ実装であり、信頼できない
    他ユーザーがいる環境での利用は想定しない。

## 主要な技術要素

- pty生成: `posix_openpt` / `grantpt` / `unlockpt` / `ptsname`、または
  `forkpty` 相当のヘルパー
- raw mode制御: termios(`tcgetattr` / `tcsetattr`、`ICANON` / `ECHO` などのフラグ操作)
- プロセス間通信: Unixドメインソケット(ストリーム型)
- デーモン化: `fork` + `setsid`、標準入出力のクローズ/リダイレクト
- ウィンドウサイズ同期: `SIGWINCH` のハンドリングと `TIOCSWINSZ` ioctl
- I/O多重化: select/poll/epollなど(OS標準の機構)で、クライアント⇔サーバー間、
  サーバー⇔pty間のデータ中継を行う

## マイルストーン

1. ptyを1つ作成し、その上でシェルを起動して、自分のターミナルからそのまま操作できる
   (サーバー/クライアント分離なしの `script` コマンド相当)
2. 上記をraw modeにして、Ctrl-Cなどの制御文字も含めてシェルにそのまま届くようにする
3. サーバー/クライアントに分離し、Unixドメインソケット経由で入出力を中継する
4. サーバーをデーモン化し、クライアントを終了(デタッチ)してもサーバー側のシェルが
   動き続けることを確認する
5. 再度クライアントを起動してアタッチし、動き続けていたシェルの出力が見えることを確認する
6. SIGWINCHのハンドリングでウィンドウサイズをptyに反映する
7. (拡張)セッション一覧・kill・複数ウィンドウ・複数ペインへ

## オープンな検討事項

- 使用言語は別途決定する
- 複数ペイン実装時のターミナルエミュレーション部分を自作するか、
  既存の軽量ライブラリ(あれば)を使うか
- Windows対応は将来的に検討するか(pty周りのAPIが大きく異なるため別途調査が必要)
